# Running Backlog

1. Add semantic validator rules beyond JSON Schema and OpenAPI structural checks.
2. Add deterministic error taxonomy with stable machine-readable reason codes.
3. Add policy conflict detection for overlapping selectors and incompatible transform chains.
4. Add stronger policy test DSL assertions, including shape, regex, exact hit counts, and expected failures.
5. Add semantic diffing between policy versions.
6. Add signed release bundles with provenance metadata.
7. Add corpus replay packs and impact analysis for rollout review.
8. Add region-aware provider resolution and trust-boundary constraints.
9. Add a release process document and versioning conventions.
10. Decide whether to formalize a `.policy-fabric/doctor` and `.policy-fabric/upgrade` workflow contract.
