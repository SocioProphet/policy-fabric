name: lattice-expanded-policy

on:
  pull_request:
    paths:
      - "examples/lattice-data-governai-expanded-policy-subjects.example.json"
      - "tools/validate_lattice_data_governai_expanded_policy_subjects.py"
      - ".github/workflows/lattice-expanded-policy.yml"
      - "Makefile"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate expanded Lattice policy pack
        run: python tools/validate_lattice_data_governai_expanded_policy_subjects.py
