# Turn note — 2026-04-02 — official AgentPlane no-touch probe and bridge contract

This turn moved the repo from AgentPlane-ready planning into an actual compatibility probe.
The main changes were:

- adding `.policy-fabric/agentplane_bridge.json` as a machine-readable bridge contract;
- adding `scripts/agentplane_probe.py` and generated probe reports;
- documenting the no-touch disposable-clone probe and the current environment block on clean npm package execution;
- updating the repo workflow so AgentPlane probe status becomes part of the standard loop.
