# Turn note — 2026-04-02 — identity and AgentPlane bridge pass

This turn reconnected the original modernization goal to the current cumulative repository state.
The main changes were:

- naming the rolling repo role **Policy Fabric Control Repository**;
- adding `AGENTS.md` as the root agent gateway;
- documenting the AgentPlane adoption path without hand-authoring a fake `.agentplane/` managed tree;
- updating ownership and doctor so the new gateway becomes an enforced part of the repo surface.
